Sure thing! To split styles.css further, I can extract sections like:
CSS variables (:root and dark-mode overrides)
Base/global element styles
Layout (grid, rows/cols)
Components (cards, tables, modals, etc.)
Utilities/overrides